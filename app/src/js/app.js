/* This is the entry point for the application.Since we are using Browserify,
you will be able to add libraries here with a require() function.

Example:

require('angular');                   //  include the AngularJS library

var app = angular.module('app', []);  //  Angular code as usual.

*/